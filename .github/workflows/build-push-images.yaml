name: Build and Push images

on:
  push:
    branches: [ "main" ]
    paths:
      - '.releaserc.json'
      - '.github/workflows/**'
      - 'images/**'

env:
  REGISTRY: ghcr.io

jobs:
  semantic-dry-run:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      packages: write
    outputs:
      new_release_published: ${{ steps.semantic_dry_run.outputs.new_release_published }}
      new_release_version: ${{ steps.semantic_dry_run.outputs.new_release_version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Semantic Release (Dry Run)
        id: semantic_dry_run
        uses: cycjimmy/semantic-release-action@v4
        with:
          dry_run: true
          working_directory: .
          extra_plugins: |
            conventional-changelog-conventionalcommits
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  docker-mpd:
    needs: semantic-dry-run
    if: needs.semantic-dry-run.outputs.new_release_published == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      packages: write
    env:
      IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/mpd
      BUILD_DIR: images/mpd
      RELEASE_VERSION: ${{ needs.semantic-dry-run.outputs.new_release_version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Ensure lowercased image name
        id: image_name
        run: |
          echo "value=$(echo "${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ${{ env.BUILD_DIR }}
          platforms: linux/amd64,linux/arm64
          file: ${{ env.BUILD_DIR }}/Dockerfile
          push: true
          tags: |
            ${{ steps.image_name.outputs.value }}:latest
            ${{ steps.image_name.outputs.value }}:${{ env.RELEASE_VERSION }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
`
  docker-mympd:
    needs: semantic-dry-run
    if: needs.semantic-dry-run.outputs.new_release_published == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      packages: write
    env:
      IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/mympd
      BUILD_DIR: images/mympd
      RELEASE_VERSION: ${{ needs.semantic-dry-run.outputs.new_release_version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Ensure lowercased image name
        id: image_name
        run: |
          echo "value=$(echo "${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ${{ env.BUILD_DIR }}
          platforms: linux/amd64,linux/arm64
          file: ${{ env.BUILD_DIR }}/Dockerfile
          push: true
          tags: |
            ${{ steps.image_name.outputs.value }}:latest
            ${{ steps.image_name.outputs.value }}:${{ env.RELEASE_VERSION }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  docker-snapserver:
    needs: semantic-dry-run
    if: needs.semantic-dry-run.outputs.new_release_published == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
      packages: write
    env:
      IMAGE_NAME: ghcr.io/${{ github.repository_owner }}/snapserver
      BUILD_DIR: images/snapserver
      RELEASE_VERSION: ${{ needs.semantic-dry-run.outputs.new_release_version }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Ensure lowercased image name
        id: image_name
        run: |
          echo "value=$(echo "${{ env.IMAGE_NAME }}" | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ${{ env.BUILD_DIR }}
          platforms: linux/amd64,linux/arm64
          file: ${{ env.BUILD_DIR }}/Dockerfile
          push: true
          tags: |
            ${{ steps.image_name.outputs.value }}:latest
            ${{ steps.image_name.outputs.value }}:${{ env.RELEASE_VERSION }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  release:
    needs:
      - semantic-dry-run
      - docker-mpd
      - docker-mympd
      - docker-snapserver
    if: needs.semantic-dry-run.outputs.new_release_published == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Semantic Release (Actual Release)
        uses: cycjimmy/semantic-release-action@v4
        with:
          working_directory: .
          extra_plugins: |
            conventional-changelog-conventionalcommits
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
