---
apiVersion: v1
kind: Service
metadata:
  name: metallb-controller
  namespace: metallb-system
spec:
  ports:
    - name: healthz
      port: 17472
      targetPort: healthz
  selector:
    app: metallb
    component: controller
  type: ClusterIP