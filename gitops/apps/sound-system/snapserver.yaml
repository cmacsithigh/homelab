---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: snapserver
  name: snapserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: snapserver
  template:
    metadata:
      labels:
        app: snapserver
    spec:
      containers:
        - image: ghcr.io/cmacsithigh/snapserver:1.1.3
          imagePullPolicy: Always
          name: snapserver
          ports:
            - containerPort: 1705
          securityContext:
            privileged: true
          volumeMounts:
            - mountPath: /fifo
              name: fifo
      hostname: snapserver
      volumes:
        - name: fifo
          persistentVolumeClaim:
            claimName: mpd-fifo