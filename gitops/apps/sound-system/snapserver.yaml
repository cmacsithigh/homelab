---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: snapserver
  name: snapserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: snapserver
  template:
    metadata:
      labels:
        app: snapserver
    spec:
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      containers:
        - image: ghcr.io/cmacsithigh/snapserver:1.1.4
          imagePullPolicy: Always
          name: snapserver
          ports:
            - containerPort: 1704
            - containerPort: 1705
            - containerPort: 1780
          securityContext:
            privileged: true
          volumeMounts:
            - mountPath: /fifo
              name: fifo
            - name: host-avahi-socket
              mountPath: /run/avahi-daemon/socket
              readOnly: true
            - name: host-dbus-socket
              mountPath: /var/run/dbus/system_bus_socket
              readOnly: true
      hostname: snapserver
      volumes:
        - name: fifo
          persistentVolumeClaim:
            claimName: mpd-fifo
        - name: host-avahi-socket
          hostPath:
            path: /run/avahi-daemon/socket
            type: Socket
        - name: host-dbus-socket
          hostPath:
            path: /var/run/dbus/system_bus_socket
            type: Socket