---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: mpd
  name: mpd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mpd
  template:
    metadata:
      labels:
        app: mpd
    spec:
      nodeSelector:
        kubernetes.io/hostname: server
      containers:
        - image: ghcr.io/cmacsithigh/mpd:1.1.1
          imagePullPolicy: Always
          name: mpd
          ports:
            - containerPort: 6600
          securityContext:
            privileged: true
          volumeMounts:
            - mountPath: /fifo
              name: fifo
            - mountPath: /var/lib/mpd/playlists
              name: mympd
              subPath: playlists
      hostname: mpd
      volumes:
        - hostPath:
            path: /fifo
            type: DirectoryOrCreate
          name: fifo
        - persistentVolumeClaim:
            claimName: mympd
          name: mympd
