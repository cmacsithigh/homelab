---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: homeassistant
  name: homeassistant
spec:
  replicas: 1
  selector:
    matchLabels:
      app: homeassistant
  template:
    metadata:
      labels:
        app: homeassistant
    spec:
      initContainers:
        - name: copy-config
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              echo "Copying Home Assistant config from ConfigMap into PVC /config";
              mkdir -p /config;
              cp -aL /tmp/homeassistant-config/. /config/;
              echo "Copy complete";
              echo "Config files in /config:";
              ls -la /config;
          volumeMounts:
            - mountPath: /config
              name: config
            - mountPath: /tmp/homeassistant-config
              name: homeassistant-config
      containers:
        - env:
            - name: TZ
              value: Europe/Dublin
          image: ghcr.io/home-assistant/home-assistant:2026.9.0
          imagePullPolicy: IfNotPresent
          name: homeassistant
          ports:
            - containerPort: 8123
          securityContext:
            privileged: true
          volumeMounts:
            - mountPath: /config
              name: config
            - mountPath: /etc/localtime
              name: localtime
              readOnly: true
            - mountPath: /run/dbus
              name: dbus
              readOnly: true
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: homeassistant
        - name: homeassistant-config
          configMap:
            name: homeassistant-config
        - hostPath:
            path: /etc/localtime
            type: File
          name: localtime
        - hostPath:
            path: /run/dbus
            type: Directory
          name: dbus